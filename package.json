{
  "name": "@nyanifold/overleafy",
  "version": "0.1.1",
  "author": "Nyanifold",
  "type": "module",
  "bin": {
    "overleafy": "./dist/cli/main.js"
  },
  "scripts": {
    "build": "tsc && node scripts/prepare-bins.mjs",
    "clean": "rm -rf dist/",
    "lint": "eslint .",
    "test": "npm run build && node --test dist/**/*.test.js",
    "prepublishOnly": "npm run build",
    "check": "npm run lint && npm test",
    "cli": "node dist/cli/main.js"
  },
  "dependencies": {
    "@napi-rs/keyring": "^1.3.0",
    "commander": "^15.0.0",
    "minimatch": "^10.2.5",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=22.14.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.31.0",
    "@types/node": "^22.20.1",
    "eslint": "^9.31.0",
    "typescript": "6.0.3",
    "typescript-eslint": "^8.38.0"
  }
}
